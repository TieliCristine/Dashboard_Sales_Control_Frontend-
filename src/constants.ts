import { ModalOptions } from "ngx-bootstrap/modal";

export const MODAL_FORM_GLOBAL_CONFIG: ModalOptions = {
  backdrop: 'static',
  keyboard: false,
  animated: true,
  class: 'modal-dialog-centered'
}
